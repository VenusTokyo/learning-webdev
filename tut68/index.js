const mod =require("./mod")//here mod contains all fn exported by the module
console.log(mod.average([2,3,4]))
//can access as an object  //as here mod has become the avg fn defined in the module
console.log("this is index.js")

